name: FV Shell Commands

on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: FV echo a string
        run: echo "hello fred"
      - name: FV multiline script
        run: |
          node -v
          npm -v
